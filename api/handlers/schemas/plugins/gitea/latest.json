{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://registry.semrel.io/schemas/plugins/gitea/latest.json",
  "title": "semrel plugin args: gitea",
  "description": "Publishes a Gitea Release on the configured instance.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "base_url": {
      "type": "string",
      "description": "Base URL of the Gitea instance."
    },
    "token": {
      "type": "string",
      "description": "Gitea API token."
    },
    "owner": {
      "type": "string",
      "description": "Repository owner. Derived from the git remote when absent."
    },
    "repo": {
      "type": "string",
      "description": "Repository slug. Derived from the git remote when absent."
    },
    "draft": {
      "type": "string",
      "description": "Create the release as a draft.",
      "default": "false",
      "enum": [
        "true",
        "false"
      ]
    }
  },
  "required": [
    "base_url",
    "token"
  ]
}
