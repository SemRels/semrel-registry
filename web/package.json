{
  "name": "go-semrel-registry-web",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Landing page and static documentation for registry.semrel.io",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "postinstall": "node ./scripts/sync-public-assets.mjs",
    "predev": "node ./scripts/sync-public-assets.mjs",
    "dev": "astro dev --host 0.0.0.0 --port 3000",
    "prebuild": "node ./scripts/sync-public-assets.mjs",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0 --port 3000",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.3.7"
  }
}