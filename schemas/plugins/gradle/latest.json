{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://registry.semrel.io/schemas/plugins/gradle/latest.json",
  "title": "semrel plugin args: gradle",
  "description": "Updates the version key in a Gradle properties file.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "file": {
      "type": "string",
      "description": "Path to the Gradle properties file.",
      "default": "gradle.properties"
    },
    "key": {
      "type": "string",
      "description": "Property key that stores the version.",
      "default": "version"
    }
  }
}
