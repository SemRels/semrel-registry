{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://registry.semrel.io/schemas/plugins/cargo/v1.json",
  "title": "semrel plugin args: cargo",
  "description": "Updates the `version` field in Cargo.toml.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "file": {
      "type": "string",
      "description": "Path to the Cargo manifest.",
      "default": "Cargo.toml"
    }
  }
}
